# ══════════════════════════════════════════════════════════════════════════════
# TEST: Avatar Upload (Partial)
# ══════════════════════════════════════════════════════════════════════════════
# Verifies avatar upload UI flow
# Note: Cannot complete actual photo upload in emulator
# Prerequisites: Logged in
# ══════════════════════════════════════════════════════════════════════════════

appId: com.iayos.app
tags:
  - profile
  - avatar
  - upload
  - partial

---

# ─────────────────────────────────────────────────────────────────────────────
# SETUP: Login as Worker
# ─────────────────────────────────────────────────────────────────────────────
- launchApp:
    clearState: true

- waitForAnimationToEnd

- tapOn:
    id: "welcome-login-button"
    optional: true

- waitForAnimationToEnd

- tapOn:
    id: "login-email-input"
- inputText: ${WORKER_EMAIL}
- hideKeyboard

- tapOn:
    id: "login-password-input"
- inputText: ${WORKER_PASSWORD}
- hideKeyboard

- tapOn:
    id: "login-submit-button"

- waitForAnimationToEnd:
    timeout: 10000

# ─────────────────────────────────────────────────────────────────────────────
# TEST: Navigate to Profile
# ─────────────────────────────────────────────────────────────────────────────
- tapOn:
    anyOf:
      - text: "Profile"
      - id: "profile-tab"

- waitForAnimationToEnd

# ─────────────────────────────────────────────────────────────────────────────
# TEST: Tap on Avatar
# ─────────────────────────────────────────────────────────────────────────────
- tapOn:
    anyOf:
      - id: "profile-avatar"
      - id: "avatar-image"
      - text: "Change Photo"
      - text: "📷"
    optional: true

- waitForAnimationToEnd

# ─────────────────────────────────────────────────────────────────────────────
# TEST: Verify Upload Options
# ─────────────────────────────────────────────────────────────────────────────
- assertVisible:
    anyOf:
      - text: "Camera"
      - text: "Gallery"
      - text: "Take Photo"
      - text: "Choose from Gallery"
      - text: "Remove"
    optional: true
    timeout: 5000

- takeScreenshot: "01_avatar_options"

# ─────────────────────────────────────────────────────────────────────────────
# TEST: Tap Camera Option (Partial - will fail gracefully)
# ─────────────────────────────────────────────────────────────────────────────
- tapOn:
    anyOf:
      - text: "Camera"
      - text: "Take Photo"
      - id: "camera-option"
    optional: true

- waitForAnimationToEnd:
    timeout: 3000

# May show camera permission or camera UI
- takeScreenshot: "02_camera_attempt"

# Cancel/close camera
- tapOn:
    anyOf:
      - text: "Cancel"
      - text: "Close"
      - text: "✕"
      - id: "close-button"
    optional: true

- waitForAnimationToEnd

# ─────────────────────────────────────────────────────────────────────────────
# TEST: Tap Gallery Option (Partial)
# ─────────────────────────────────────────────────────────────────────────────
- tapOn:
    anyOf:
      - id: "profile-avatar"
      - text: "Change Photo"
    optional: true

- waitForAnimationToEnd

- tapOn:
    anyOf:
      - text: "Gallery"
      - text: "Choose"
      - text: "Photos"
      - id: "gallery-option"
    optional: true

- waitForAnimationToEnd:
    timeout: 3000

- takeScreenshot: "03_gallery_attempt"

# Close gallery picker
- tapOn:
    anyOf:
      - text: "Cancel"
      - text: "Close"
      - text: "Back"
    optional: true

- waitForAnimationToEnd

# ─────────────────────────────────────────────────────────────────────────────
# NOTE: Cannot complete avatar upload in E2E test
# This test verifies the upload UI is accessible
# ─────────────────────────────────────────────────────────────────────────────

- takeScreenshot: "04_completed"

# ══════════════════════════════════════════════════════════════════════════════
# TEST: Save and Unsave Job (Worker)
# ══════════════════════════════════════════════════════════════════════════════
# Verifies save job functionality
# Prerequisites: Logged in as WORKER, at least one job exists
# ══════════════════════════════════════════════════════════════════════════════

appId: com.devante.iayos
tags:
  - jobs_worker
  - save
  - bookmark

---

# ─────────────────────────────────────────────────────────────────────────────
# SETUP: Login as Worker
# ─────────────────────────────────────────────────────────────────────────────
- launchApp:
    clearState: true

- waitForAnimationToEnd

- tapOn:
    id: "welcome-login-button"
    optional: true

- waitForAnimationToEnd

- tapOn:
    id: "login-email-input"
- inputText: ${WORKER_EMAIL}
- hideKeyboard

- tapOn:
    id: "login-password-input"
- inputText: ${WORKER_PASSWORD}
- hideKeyboard

- tapOn:
    id: "login-submit-button"

- waitForAnimationToEnd:
    timeout: 10000

# ─────────────────────────────────────────────────────────────────────────────
# TEST: Navigate to Jobs
# ─────────────────────────────────────────────────────────────────────────────
- tapOn:
    text: "Jobs"
    optional: true

- waitForAnimationToEnd

# ─────────────────────────────────────────────────────────────────────────────
# TEST: Open a Job Detail
# ─────────────────────────────────────────────────────────────────────────────
- tapOn:
    id: "job-card-0"
    optional: true
      - text: "₱"
    index: 0
    optional: true

- waitForAnimationToEnd:
    timeout: 5000

# ─────────────────────────────────────────────────────────────────────────────
# TEST: Save the Job
# ─────────────────────────────────────────────────────────────────────────────
- tapOn:
    id: "job-card-0"
    optional: true
      - text: "Save"
      - text: "♡"
      - text: "🤍"
    optional: true

- waitForAnimationToEnd

# Verify saved state
- assertVisible:
    text: "Saved"
    optional: true
    timeout: 5000

- takeScreenshot: "01_job_saved"

# ─────────────────────────────────────────────────────────────────────────────
# TEST: Go Back and View Saved Jobs
# ─────────────────────────────────────────────────────────────────────────────
- tapOn:
    id: "job-card-0"
    optional: true
      - text: "←"
    optional: true

- waitForAnimationToEnd

# Navigate to saved jobs
- tapOn:
    id: "job-card-0"
    optional: true
      - text: "Saved"
      - text: "❤️"
    optional: true

- waitForAnimationToEnd

# Verify saved job appears in list
- assertVisible:
    text: "Saved Jobs"
    optional: true
    timeout: 10000

- takeScreenshot: "02_saved_jobs_list"

# ─────────────────────────────────────────────────────────────────────────────
# CLEANUP: Unsave the Job
# ─────────────────────────────────────────────────────────────────────────────
- tapOn:
    id: "job-card-0"
    optional: true
      - text: "₱"
    index: 0
    optional: true

- waitForAnimationToEnd

- tapOn:
    id: "job-card-0"
    optional: true
      - text: "Unsave"
      - text: "❤️"
      - text: "♥"
    optional: true

- waitForAnimationToEnd

- takeScreenshot: "03_job_unsaved"

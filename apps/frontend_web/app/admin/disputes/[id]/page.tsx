// Re-export the backjobs detail page for the new /admin/disputes/[id] URL.
// The backjobs [id] page was already linked from sidebar link references.
export { default } from "../../jobs/backjobs/[id]/page";

# =============================================================================
# iAyos Monorepo - .dockerignore
# =============================================================================
# Excludes files not needed in Docker build context for smaller/faster builds
# =============================================================================

# Node modules (rebuilt in container)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build outputs (rebuilt in container)
.next/
out/
dist/

# Python virtual envs (rebuilt in container)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
*.egg-info/
.pytest_cache/
.coverage

# IDEs & Editors
.vscode/
.idea/
*.swp
*.swo
*~
*.iml

# OS files
.DS_Store
Thumbs.db

# Environment files (use docker-compose env_file or secrets)
.env
.env.local
.env.production
.env.staging
!.env.docker
!.env.example
!.env.*.example

# Git
.git/
.gitignore
.github/

# Documentation (not needed in runtime)
docs/
*.md
!README.md

# Testing (not needed in production image)
coverage/
.nyc_output/
htmlcov/

# Turbo cache
.turbo/

# Logs
*.log
logs/

# Temp files
tmp/
temp/
*.tmp

# Database files (use volumes)
*.db
*.sqlite
*.sqlite3
*.sql

# Media files (use volumes)
media/
uploads/

# Security scan outputs
*.sarif
vulnerability-reports/
security-scans/
docker-scout-*.json

# Scripts and tests (not needed in production)
apps/backend/scripts/
apps/backend/backups/

# Mobile app (not needed in backend/frontend images)
apps/frontend_mobile/

# Claude/AI assistant files
.claude/
.playwright-mcp/
CLAUDE.md
AGENTS.md
.mcp.json

# Datasets (large files, mount as volume if needed)
apps/backend/scripts/ml/Datasets/
